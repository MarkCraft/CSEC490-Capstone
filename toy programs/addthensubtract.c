#include <stdio.h>

int main(){
    int first;
    int second;
    int third;
    printf("Enter an integer: ");
    scanf("%d", &first);
    printf("Enter another integer: ");
    scanf("%d", &second);
    int multiply = first * second;
    int difference = third - multiply;
    printf("Answer = %d", difference)

    return 0;
}